// Importing the React library
import React from "react";

// Creating a React context for managing user-related data
const UserContext = React.createContext("");

// Exporting the UserContext to make it available for other components
export default UserContext;


//TODO: Make it so that the username is a global variable that can be accessed by all components